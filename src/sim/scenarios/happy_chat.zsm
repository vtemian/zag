# Canary scenario: a clean chat turn should pass exit 0. If this fails,
# the harness is broken — investigate before trusting the segfault scenario.

spawn ./zig-out/bin/zag
wait_text /Welcome to zag/
send "hello" <Enter>
wait_idle 5s
snapshot final
